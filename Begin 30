/*Begin 30
Дано значенння кута α в радіанах (0 < α < 2·π). Визначити значення
цього кута в градусах, враховуючи, що 180° = π радіан. Значения π 
використовувать 3,14.
*/

#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	cout << "Введіть значення кута в радіанах" << endl;
	double a;
	cin >> a;
	a = a * 180.0 / 3.14;
	cout << a << endl;
	return 0;
}
